<title>Driver Notification Changes</title>
# تعديلات إشعارات السائق — ملخص

## 1) إشعارات جديدة (Added)

### أ) العميل عدّل الطلب والسائق لسه ما قبلش التعيين
لو السائق متعيّن على استلام أو توصيل (`driver_pickup_assigned` /
`driver_delivery_assigned`) ولسه ما قبلش، وبعدين العميل عمل تعديل على
الطلب — قبل كده السائق مكنش بياخد أي إشعار خالص. دلوقتي بيوصله:

- **استلام**: "تم إلغاء طلب الاستلام لوجود تعديلات من قبل العميل."
- **تسليم**: "تم إلغاء طلب التسليم لوجود تعديلات من قبل العميل."

**النوع (type)**: `driver_pickup_cancelled_client_edit` /
`driver_delivery_cancelled_client_edit`
**القناة**: Push فقط (Firebase) — بدون SMS.
**الكود**: `Modules/Order/app/Http/Controllers/Api/V1/User/OrderTrackingController.php`
(داخل `updateOrder()`، بعد الـ commit مباشرة).

### ب) المغسلة غيّرت السائق والسائق القديم لسه ما قبلش
كان فيه إشعار عام بالفعل ("تم إلغاء تعيينك... وتحويله لسائق آخر")،
بس نفس النص كان بيتبعت بغض النظر عن السبب. دلوقتي لو المغسلة نفسها
هي اللي غيّرت السائق، الرسالة بقت محددة:

- **استلام**: "تم إلغاء طلب الاستلام من قبل المغسلة."
- **تسليم**: "تم إلغاء طلب التسليم من قبل المغسلة."

(لو التغيير حصل من الأدمن بدل المغسلة، الرسالة العامة القديمة لسه
شغالة زي ما هي.)

**النوع (type)**: `driver_pickup_unassigned` / `driver_delivery_unassigned`
(الاسم زي ما كان، بس النص اتغيّر لما يكون المصدر "vendor")
**القناة**: Push فقط (Firebase) — بدون SMS.
**الكود**: `app/Listeners/SendDriverAssignmentNotification.php`،
`app/Events/DriverAssigned.php` (ضفنا `actorType`)،
`app/Services/OrderStatusService.php` (بنبعت الـ actorType للـ event).

---

## 2) إشعارات اتلغت (Removed)

### السائق قبِل الاستلام أو التوصيل
قبل كده لما السائق يقبل التعيين، العميل كان بياخد إشعار
("قبل السائق استلام طلبك" / "قبل السائق توصيل طلبك"). ده اتشال
خالص — العميل مالوش أي Action يعمله في اللحظة دي، والإشعار المهم
ليه هو لما السائق يبقى "في الطريق" فعلاً (ده لسه موجود زي ما هو).

**إشعار المغسلة/الأدمن على نفس الحدث لسه موجود ومتأثرش.**

**الكود**: `app/Listeners/SendOrderStatusNotification.php`
(`onDriverPickupAccepted()`, `onDriverDeliveryAccepted()`)

---

## 3) اتأكد إنه شغال بالفعل (مفيش تعديل مطلوب)

بمراجعة الكود، الحاجات دي كانت موجودة من قبل ومحتاجتش أي تعديل:

- العميل يقبل أو يرفض تعديلات المغسلة (Branch Review) → **المغسلة**
  بتاخد إشعار في الحالتين ("وافق العميل" / "رفض العميل وتم إلغاء
  الطلب").
- السائق يبلّغ إنه "في الطريق" → **العميل** بياخد إشعار.
- العميل يأكّد جاهزيته (Visit Response) أو يأجّل → **السائق
  والمغسلة** بياخدوا إشعار في الحالتين.

---

## ملاحظة عن القناة (SMS)
كل الإشعارات الجديدة/المعدّلة في القسم (1) اتحطّت في قايمة استثناء
(`NotificationSmsService::PUSH_ONLY_TYPES`) عشان تفضل Push بس —
مش هيتبعتلها SMS حتى لو إعدادات الـ SMS العامة شغالة لكل الأنواع،
لأنها إشعارات بتتكرر بكثرة ومش بتستاهل تكلفة SMS في كل مرة.

## الكوميتات
- `a97a3db` — إضافة الإشعارين الجديدين (1أ و1ب)
- `7278e4c` — Push-only + تعديل "توصيل" لـ"تسليم"
- `e9caa09` — إلغاء إشعار العميل عند قبول السائق
